import React from "react";
import { Apple, ChevronDown, Ham, Coffee, Cat, SprayCan, Candy, CookingPot, Soup, Wine, ShoppingBag } from "lucide-react";
import { Link } from "react-router-dom";

const Cart = ({ cart, setCart }) => {
  

  return (
   <></>
  );
};

export default Cart;